export default ({app}, inject) => {
    inject('utils', {
        /**
         * @method
         * @param {Type} authCode 目标对象
         * @returns {Type} 运营商名称
         * @desc 根据目标对象获取运营商
         */
        verifyAuth: (authCode) => {

        }
    });

}